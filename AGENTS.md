# PeiXiu 项目规则入口

## 作用与资料

本文件只规定 PeiXiu 的项目级约束和风险触发入口。

文档按变更需要阅读：

- [README](README.md)：项目定位、产品边界和当前状态。
- [实施方案](docs/guangzhou-motorcycle-routing-implementation-plan.md)：技术设计和验收标准。
- `docs/agent-workflow.md`：Issue、状态、分支和 Review 的详细流程（如已建立）。
- `docs/runbook/`：部署、回滚和事故处理说明（如已建立）。

## 项目不可变约束

- V1 保持广州范围，不擅自扩大产品边界。
- 路线计算保持浏览器本地优先，不未经决策引入远程 Routing API。
- `motorcycle` 不得静默降级为其他车型。
- 不把“避开已知摄像头点位”描述为合法通行、安全保证或法律判断。
- 涉及上述约束的改变，必须在 Issue 中记录决策并按高风险变更处理。

## 风险触发入口

- 小修改：直接编辑，执行与修改相称的验证；不强制 Issue、分支或 Review。
- 普通或非平凡工作：建立轻量 Issue，记录范围、验收标准和验证方式。
- 高风险变更：先完成设计、依赖和回滚考虑，再使用分支并进行 Review。
- 事故：优先止损、隔离或回滚，随后按 Runbook 补充记录和复盘。

完整工作流以 `docs/agent-workflow.md` 和全局风险分级 SDLC 为准，不在本文件重复状态机。

## 验证与授权

- 验证深度必须与变更风险相称；不要求所有修改运行完整测试、构建和冒烟测试。
- 完成前说明已执行的验证、未验证内容和已知限制。
- 不自动创建分支、提交、推送、合并、部署或修改生产数据。
- 远程、生产和破坏性操作必须获得当前请求中的明确授权。
- 不自动读取被 Git 忽略的 `.codex/local-instructions.md`；只有在明确进行部署或机器相关操作且获得授权后，才按需读取。
